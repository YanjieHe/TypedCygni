enum OpCode {
PUSH_INT_1BYTE
PUSH_INT_2BYTE,
PUSH_INT,

PUSH_DOUBLE_0,
PUSH_DOUBLE_1,
PUSH_DOUBLE,
PUSH_NULL,

PUSH_STACK,
POP_STACK,
PUSH_STATIC,
POP_STATIC,
PUSH_CONSTANT,
POP_CONSTANT,
PUSH_ARRAY,
POP_ARRAY,
PUSH_FIELD,
POP_FIELD,
ADD,
SUB,
MUL,
DIV,
MOD,
BIT_AND,
BIT_OR,
BIT_XOR,
MINUS,
BIT_NOT,
CAST,
UP_CAST,
DOWN_CAST,
EQ,
NE,
GT,
LT,
GE,
LE
}